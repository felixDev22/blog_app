<%= form_with model: post, url: user_posts_path, local: true, class: "form" do |form| %>
  <div class="form-input">
    <%= form.label :title  %>
  <%= form.text_field :title, required: true, class: "form_title" %>
</div>
<div class="form-input">
  <%= form.label :text %>
<%= form.text_area :text, required: true, class: "form_text" %>
</div>
<div class='submit-btn'>
  <%= form.submit "Submit post", class: "btn" %>
</div>
<% end %>